import 'package:flutter/material.dart';

/// Colores oficiales de MiSalud_VortexIT.
/// Todos los colores de la aplicación deben obtenerse desde esta clase.
class AppColors {
  AppColors._();

  // Colores principales
  static const Color primary = Color(0xFF006A6A);
  static const Color secondary = Color(0xFF4A6363);

  // Fondos
  static const Color background = Color(0xFFF4F8F8);
  static const Color surface = Colors.white;

  // Estados
  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFF9A825);
  static const Color danger = Color(0xFFBA1A1A);
  static const Color info = Color(0xFF1565C0);

  // Texto
  static const Color textPrimary = Color(0xFF1B1B1B);
  static const Color textSecondary = Color(0xFF5F6368);

  // Bordes
  static const Color border = Color(0xFFD9E2E2);

  // Tarjetas
  static const Color card = Colors.white;
}
